#!/usr/bin/node


var program = require('commander');

program
  .version('0.0.1' )
  .command('identify', 'identify the package on the current working directory')
  .command('run [options] <file>', 'run the project?')
  .parse(process.argv)
;
